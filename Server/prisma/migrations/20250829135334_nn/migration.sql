/*
  Warnings:

  - You are about to drop the column `postal_code` on the `service_provider` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "service_provider" DROP COLUMN "postal_code";
